# RealGram infra migration + rebrand — task split

Tracking issue: [`XS227/SetaLink#17`](https://github.com/XS227/SetaLink/issues/17) — status updates should still be posted there until it's closed, since that's where Khabat and other agents are watching. This file is the working task list; the issue is the human-facing status feed. Keep both in sync (pattern borrowed from `XS227-BIAP/PROJECT_STATUS.md`).

**Repo direction (2026-08-31):** `Real-Gram/Realgram` is now the canonical repo going forward. `XS227/SetaLink` is being retired as a name — new work (code, and eventually infra naming) should not introduce new "setalink" references.

## Two boxes involved

| Box | Hostname | Role today |
|---|---|---|
| `5.249.252.221` | `vps-5348441` (Uniweb, 1 vCPU/1GB RAM) | Current prod: setalink.no website (nginx+PHP-FPM), `data/analytics.db`, its own Xray/VPN exit node, calling-relay, Nasrin's BIAP Expo tunnel, cron jobs. RAM upgrade stuck since 2026-08-25 (no Uniweb API/CLI access) — this is *why* we're moving off it. |
| `5.249.252.88` | `vps-5359843` | Target box. Already runs: `desktop.realgram.no` (frontend + pm2 backend), a **second, independent** Xray/VPN edge on `vpn.setalink.no`, `calling-relay`, and unrelated `biap-fin.service`. |

Whoever is running an agent session on a given box should work the rows below that say "access needed: that box" — cross-box file transfers need explicit coordination (ssh key exchange or a human-run `scp`), don't assume one agent can reach the other box directly.

## Work item A — finish moving `.221` → `.88`

Access needed: **`.221`** for export steps, **`.88`** to receive.

- [x] **Export `data/analytics.db`** — schema/size confirmed 2026-08-31 (from `.221`): 9.3MB, 101 tables, 194 rows in `devices`, 22,622 in `app_events`. Small enough for a straight `scp` + point the service at the copy, no migration tooling needed. Not yet actually copied to `.88` — next step for whoever has write access on `.88`'s end.
- [x] Inventory `.221`'s nginx+PHP-FPM setalink.no site (webroot `/var/www/setalink`) — **Inventory done 2026-08-31:** `sites-enabled/` on `.221` has `api.setalink.no`, `setalink-landing` (the main site), `calling-relay-ws`, `default` — plus, worth flagging, **`app.dadashi.no`** (Dr. Nasrin Dadashi's own unrelated site, also hosted on this box — needs its own decision if `.221` gets decommissioned, out of scope for this migration otherwise). PHP-FPM: single pool, `www.conf`.
  - **Khabat's decision (2026-08-31): the `setalink-landing` public site should redirect to `realgram.no`.**
  - **Correction (2026-08-31, peer session on `.221` caught this before anything was touched):** the bare `setalink.no` domain — served by the *same* `setalink-landing` vhost slated for the redirect — is hardcoded as `BASE_URL` across nearly the entire mobile app backend surface, not just `/api.php`: `remoteConfigService.ts`, `profileBundleService.ts`, `entitlementService.ts`, `realWalletService.ts`, `ssoService.ts`, `referralEarningsService.ts`, `handleService.ts`, `tapAnalytics.ts`, `usageHistoryService.ts`, `zarSyncService.ts`, and `vpnBridge.ts`'s tunnel-log reporting all hit it directly (confirmed by grepping `mobile-app/src/services/*.ts` for the literal domain). The same vhost also proxies `/ws/call` (calling-relay) and has a `/_setalink-admin/` path. `api.setalink.no` (a *different*, separate vhost) only serves `/v1/*` and was never the concern.
  - **Re-confirmed scope (2026-08-31): redirect is per-path, not domain-level.** Only the human-facing marketing pages (`/`, `/blog`, `/faq`, etc.) on `setalink-landing` redirect to `realgram.no`. `/api.php`, `/ws/call`, and `/_setalink-admin/` on that same vhost stay completely untouched — this is a handful of specific `location` blocks getting a redirect, not a `server_name`-level or catch-all change.
  - **[x] Marketing-page redirect: DONE and LIVE on `.221`** (2026-08-31) — `location = /` → `301 https://realgram.no/`, `location = /faq` + `/faq.php` → `301 https://realgram.no/faq.html`. Allowlist approach, nothing else touched; vhost backed up before editing, `nginx -t` clean before reload. Verified directly against the vhost: `/api.php` (200), `/v1/servers` (401, unchanged), `/ws/call` (404, unchanged, calling-relay's separate concern), `stats.php`/`real-ssh.php`/`vps-helper.php`/`sitemap.php` all unchanged.
  - **Resolved cross-session conflict, same day:** this doc briefly said to hold the redirect entirely pending the full BASE_URL migration below, based on what Khabat told the `.88` session — but Khabat separately and directly confirmed to the `.221` session that the path-scoped redirect should proceed now, independent of that larger migration. The `.221` session went with the direct confirmation (correctly) and it's live and staying; the "hold off" note above is superseded for the marketing-page redirect specifically.
  - **Still separate and not started:** the actual **full-domain** redirect of `setalink.no` → `realgram.no` (everything, including `/api.php`) — only possible once the app stops hardcoding `setalink.no/api.php` as `BASE_URL`. Prep sequence (mirrors the `vpn.realgram.no` parallel-cutover pattern from Work item B): stand up `api.realgram.no` serving the same API in parallel, verify it, then flip `mobile-app/src/services/*`'s `BASE_URL` to it (app-release required — compiled in, unlike the VPN node catalog/edge_host). This piece can proceed at whatever pace makes sense; it does not front-run or get front-run by the marketing redirect above.
- [x] **Inventory `.221`'s own Xray/VPN exit node config — DONE, with a clear recommendation.** `.221` runs a live `xray.service` (systemd, active) with the *identical* inbound layout to `.88`'s edge: `vless` on `ws:10000` / `xhttp:10001` / `httpupgrade:10002`, plus a Reality inbound on `:8443` decoy-SNI'd as `www.cloudflare.com`/`www.microsoft.com`. nginx's own `stream{}` map on `.221` still has a `vpn.setalink.no → 127.0.0.1:4434` entry too. **But it's very likely dead:** `dig vpn.setalink.no` already resolves to `5.249.252.88`, not `.221`, so no real traffic reaches `.221`'s copy of that SNI; it's also not registered under any node id in `public/v1.php` (checked every node's `address` field — no match for `5.249.252.221`), so the app's server list never hands it out either. `/var/log/xray/access.log` for the last ~1.5h shows *zero* connections on the ws/xhttp/httpup/reality inbounds — the only entries are `.221`'s own internal health-check pinging `api` on `127.0.0.1:8344` every 5 minutes. Recommendation: this looks like orphaned infra (maybe a predecessor of `.88`'s edge, or a same-box test that was never fully decommissioned) that's safe to shut down — but per the guardrail below, that's Khabat's call, not made here.
- [x] **Inventory the node-health / real-ssh-worker cron jobs — DONE, and there's more than the plan named.** Found via `crontab -l` (root) + `/etc/cron.d/` + `systemctl list-timers`, all on `.221`:
  - Root crontab (all critical to the live VPN service, all currently only running on `.221`):
    - `*/5 * * * *` — `poll-traffic.sh` (xray traffic stats)
    - `*/2 * * * *` — `parse-last-seen.sh` (device `last_seen_at` from access log)
    - `*/2 * * * *` — `scripts/export-xray-stats.sh` (admin-dashboard xray stats)
    - `*/2 * * * *` — `scripts/check-node-health.sh` (writes `data/node_health.json` — this is the one the plan named)
    - `*/10 * * * *` — `scripts/update-real-rate.php` (REAL/USD rate for premium pricing)
    - `0 5 * * *` — `scripts/sync-adsgram-daily.php` (AdsGram revenue sync)
  - `/etc/cron.d/` (ubuntu user):
    - `* * * * *` — `real-ssh-worker.php` (the one the plan named — drains `real_ssh_devices` queue into `authorized_keys`)
    - `* * * * *` — `vps-helper-worker.php` (drains `vps_helpers` queue)
    - `17 6 * * *` — `gsc_cron.php` (daily Search Console → keyword_ranks sync)
  - systemd timer: `setalink-watchdog.timer` (fires every ~1 min, `setalink-watchdog.service`)
  - None of these have been recreated on `.88` yet — whoever has `.88` access needs to check which are already superseded there (e.g. `.88` almost certainly needs its own node-health probe once it's carrying real traffic) vs. which need a straight port.
- [x] **Confirm Nasrin's BIAP Expo tunnel status — DONE.** Still actively running on `.221` (`ps aux` shows `expo start --tunnel --clear` + the ngrok helper process, started 2026-08-25, PM2-managed under the `nasrin` user) — **not** moved to `.88`. Do not touch/kill it here.

## Work item B — rebrand `vpn.setalink.no` → realgram-branded domain

Access needed: **`.88`** only — this edge (nginx stream SNI router + Xray) lives entirely there. Do **not** start this until Work item A's exit-node inventory above clarifies whether `.221` has a competing/duplicate edge that also needs folding in first.

Current setup on `.88` (confirmed 2026-08-31):
- Public `:443` is a single nginx `stream{}` block in `/etc/nginx/nginx.conf` that SNI-routes by `$ssl_preread_server_name`:
  - `www.cloudflare.com` / `www.microsoft.com` → `127.0.0.1:8443` (Xray Reality inbound — this is SNI-masquerading for censorship resistance, **not** the "setalink" brand; leave these serverNames alone, they're decoys, not the product name)
  - `vpn.setalink.no` → `127.0.0.1:4434` (nginx TLS termination, vhost `/etc/nginx/sites-available/vpn-setalink`) → proxies `/ws`, `/xhttp/`, `/httpup` to loopback Xray inbounds on `10000`/`10001`/`10002`
  - `default` → `127.0.0.1:4430` (the shared vhost port other sites use)
- Real users' client configs for the WS/XHTTP/HTTPUpgrade transports hardcode `vpn.setalink.no` as the TLS SNI + cert domain. **Renaming this in place breaks every distributed client config the moment DNS/cert changes** — this must be a parallel cutover, never an in-place rename.

Proposed sequence (none of this touches the live domain until the last step):

1. [x] **Domain picked and confirmed by Khabat: `vpn.realgram.no`** (same `realgram.no` zone as `desktop.realgram.no`).
2. [x] **DNS A record added** — `vpn.realgram.no` → `5.249.252.88`, propagated (confirmed via 1.1.1.1 + 8.8.8.8).
3. [x] **Cert issued** — Let's Encrypt, webroot method, ECDSA, `/etc/letsencrypt/live/vpn.realgram.no/`, expires 2026-11-29.
4. [x] **New nginx `server{}` block deployed** — `vpn-realgram` vhost added on `127.0.0.1:4434`, same loopback Xray targets as `vpn-setalink`. Repo-managed as of this update: `infra/nginx/vpn-realgram.conf` + `scripts/deploy-vpn-realgram.sh` in **this repo** (`Real-Gram/Realgram`, not `XS227/SetaLink` — Khabat's call: all new infra tooling goes in the new repo only). **Correction:** the `XS227/SetaLink` pattern this mirrors (`deploy-vpn-edge.sh` + `infra/nginx/vpn-setalink.conf`) turned out to live on an *unmerged* branch (`fix/vpn-migration-routing`, 2 commits, deploy script never actually run) whose target box Khabat doesn't remember — decision (2026-08-31): leave that branch untouched, don't merge/deploy/delete it, until clarified. Unlike that branch, this repo's `vpn-realgram` files describe a change that *was* actually applied and verified live on `.88` (steps 5-6 below) — the files were added here for the record/reproducibility, not as an unverified proposal.
5. [x] **`stream{}` map entry added** — `vpn.realgram.no 127.0.0.1:4434;` alongside the existing `vpn.setalink.no` line.
6. [x] **Verified end-to-end, strictly (no `-k`/skip-verify):** correct cert CN served per domain (no cross-contamination), and `/ws`/`/xhttp/`/`/httpup` return identical status codes (400/400/502) on both `vpn.realgram.no` and `vpn.setalink.no` — new edge behaves exactly like the production-proven old one. Both domains live in parallel; `vpn.setalink.no` untouched.
7. **Correction to how this step actually works, found during client-code review:** the edge domain (`vpn.setalink.no`/`edge.setalink.no`) is *not* what determines which server a user connects to — that's controlled by the **node catalog** the app fetches live from `/v1/servers` (`v1.php`). No app release is needed either way. **Live snapshot 2026-08-31:** `remote-config`'s active `edge_host` is `edge.setalink.no` (→ `.221`, the box being retired), *not* `vpn.setalink.no` — `vpn.setalink.no` is only listed as a `profile-bundle` backup domain today.
   - **`v1.php` git-vs-live discrepancy — needs resolving by whoever has `.221` filesystem access before registering anything:** `public/v1.php` IS tracked in `XS227/SetaLink` (confirmed via `git show`, contrary to an earlier assumption in this doc that it was server-only/untracked — it just wasn't materialized in one partial clone). But the **git copy has no `.88` node at all** — no `5.249.252.88` anywhere, no function matching a `cph2` node id (only `v1_primary_node`, `v1_helsinki_node`, `v1_germany_node`, `v1_proisp_node`, `v1_cfedge_node`, `v1_starlink_nodes`). A peer session read a `cph2` node off the box directly and reported it points at `5.249.252.88` — since the git file doesn't have it, that node must exist only in `.221`'s **live deployed** `public/v1.php`, which is already known to carry uncommitted hotfixes (see this repo's own history of direct-live-file patches). **Don't trust the git checkout alone for this — read the live file on `.221` before deciding whether `cph2`'s creds already match the `vpn-realgram`/`vpn-setalink` edge or whether a fresh node entry is needed.**
   - **Khabat's decision (2026-08-31): make `vpn.realgram.no` available to *all* users right away from the new server — not gated to new users/devices only.** Whoever has `.221`/`v1.php` access: register `vpn.realgram.no` as a node (new id, or update `cph2` if its live creds already match this edge — confirm against the live file first, not just git) and make it generally available, not behind a rollout/new-user-only flag. Remember to commit the change back to git too, matching the file's own documented live-patch-then-commit pattern — don't leave it as another live-only drift.
8. Sunset window: watch traffic/logs on `vpn.setalink.no`'s SNI (and `edge.setalink.no` once/if that also moves). Once near zero for a safe window (propose ≥2–4 weeks, confirm with Khabat), remove the old nginx `server{}` block, its `stream{}` map line, let its cert expire, and drop its DNS record. Note `setalink.no` itself (the API host, hardcoded in the app's compiled source) is a separate, app-release-requiring rename — out of scope for this edge-only cutover.
9. Only after step 8 is `vpn.setalink.no` actually gone from `.88` — that's when this box's side of the rebrand is complete, not before.

## Guardrails

- Nothing in Work item B touches a live client-facing domain before step 6's parallel verification passes. If in doubt, stop and post status to issue #17 rather than guessing.
- `.221` is live prod with real Iranian users depending on its VPN tunnel — no destructive action there (service stops, file deletes, cron removal) without Khabat's explicit go-ahead in the issue or directly.
- Keep this file and issue #17 updated as steps complete — the next agent picking this up should be able to `git pull` this repo and read the issue rather than re-discovering state from scratch.
